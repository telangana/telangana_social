<%= form_for(@fb_token) do |f| %>
  <% if @fb_token.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fb_token.errors.count, "error") %> prohibited this fb_token from being saved:</h2>

      <ul>
      <% @fb_token.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :access_token %><br>
    <%= f.text_field :access_token %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
